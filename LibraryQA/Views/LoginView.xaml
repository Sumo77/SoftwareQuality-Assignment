<UserControl x:Class="LibraryQA.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid Background="#F5F5F5">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="280">
            <TextBlock Text="Library System" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Username" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Margin="0,0,0,12" Padding="6"/>

            <TextBlock Text="Password" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,12" Padding="6"/>

            <TextBlock Text="Log in as" Margin="0,0,0,4"/>
            <ComboBox x:Name="RoleComboBox" Margin="0,0,0,16" SelectedIndex="0">
                <ComboBoxItem Content="Member"/>
                <ComboBoxItem Content="Staff"/>
            </ComboBox>

            <Button x:Name="LoginButton" Content="Log In" Padding="8" Click="LoginButton_Click"/>

            <TextBlock x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap"
                       Margin="0,12,0,0" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</UserControl>
