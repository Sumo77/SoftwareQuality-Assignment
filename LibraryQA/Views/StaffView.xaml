<UserControl x:Class="LibraryQA.Views.StaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#9F4E2E" Padding="12">
            <Grid>
                <TextBlock Text="Staff Dashboard" Foreground="White" FontSize="18"
                           FontWeight="Bold" VerticalAlignment="Center"/>
                <Button x:Name="LogoutButton" Content="Log Out" HorizontalAlignment="Right"
                        Padding="8,2" Click="LogoutButton_Click"/>
            </Grid>
        </Border>

        <TabControl Margin="12">
            <TabItem Header="Issue / Return">
                <StackPanel Margin="12">
                    <TextBlock Text="Member ID" Margin="0,0,0,4"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBox x:Name="MemberIdBox" Width="150" Padding="6" Margin="0,0,8,0"/>
                        <TextBox x:Name="BookIdBox" Width="150" Padding="6" Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Issue Loan" Padding="8,4" Margin="0,0,8,0" Click="IssueButton_Click"/>
                        <Button Content="Process Return" Padding="8,4" Click="ReturnButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <TabItem Header="Overdue Items">
                <ListView x:Name="OverdueListView" Margin="12">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Title" Width="220" DisplayMemberBinding="{Binding Title}"/>
                            <GridViewColumn Header="Member" Width="150" DisplayMemberBinding="{Binding MemberName}"/>
                            <GridViewColumn Header="Due Date" Width="120" DisplayMemberBinding="{Binding DueDate}"/>
                            <GridViewColumn Header="Days Overdue" Width="120" DisplayMemberBinding="{Binding DaysOverdue}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>

            <TabItem Header="Reservations">
                <ListView x:Name="ReservationsListView" Margin="12">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Title" Width="220" DisplayMemberBinding="{Binding Title}"/>
                            <GridViewColumn Header="Member" Width="150" DisplayMemberBinding="{Binding MemberName}"/>
                            <GridViewColumn Header="Reserved On" Width="150" DisplayMemberBinding="{Binding ReservedOn}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>

            <TabItem Header="Reporting">
                <StackPanel Margin="12">
                    <TextBlock Text="Summary Statistics" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TotalOnLoanText" Text="Items currently on loan: -"/>
                    <TextBlock x:Name="TotalOverdueText" Text="Items overdue: -" Margin="0,4,0,0"/>
                    <TextBlock x:Name="TotalReservationsText" Text="Active reservations: -" Margin="0,4,0,0"/>

                    <TextBlock Text="Most Borrowed Titles" FontWeight="Bold" FontSize="14" Margin="0,16,0,8"/>
                    <ListView x:Name="MostBorrowedListView" Height="180">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Title" Width="250" DisplayMemberBinding="{Binding Title}"/>
                                <GridViewColumn Header="Times Borrowed" Width="150" DisplayMemberBinding="{Binding TimesBorrowed}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</UserControl>
